[pytest]
# The pytest suite lives in tests/. Scoping collection here keeps a bare
# `python -m pytest` clean and prevents it from importing top-level run
# scripts (e.g. main.py) during collection.
testpaths = tests
