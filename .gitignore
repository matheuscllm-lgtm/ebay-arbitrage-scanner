.venv/
__pycache__/
*.pyc
.pytest_cache/

# Local data and scan output (never publish deal lists)
data/
data/cache/
results/
*.csv
*.xlsx
*.json

# Local strategy / method / target list (kept out of the public repo)
METODO.md
# watchlist.yaml e GERADA e versionada (build_watchlist.py; operador 2026-09-03)

# Local-only credentials
.env

# fixtures reais de teste (JSON da Browse API) precisam entrar no repo
!tests/fixtures/*.json

# Public generator inputs required in a fresh clone (not scan results).
!src/catalog/set_catalog.json
!src/catalog/iconic_pokemon.csv
